import React from 'react';
import TopHeader from '../Dashboard/TopHeader';

/**
 * Header component that serves as a wrapper for the TopHeader.
 * The actual header content and styling are defined within TopHeader.
 * This component maintains a clean layout structure.
 */
const Header: React.FC = () => {
  return <TopHeader />;
};

export default Header;
